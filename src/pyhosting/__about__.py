"""The `__about__` module exposes the version of the `pyhosting` package:

Example:

```python
from pyhosting.__about__ import __version__
print(__version__)
```

Note that container version can also be imported directly from `pyhosting` package:

Example:

```python
from pyhosting import __version__
print(__version__)
```
"""
__version__ = "0.1.0"
